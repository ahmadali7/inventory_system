<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Name</th>
      <th>Category</th>
      <th>Price</th>
      <th>Cost</th>
      <th>Discount</th>
      <th>Quantity</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td>
          <%= image_tag product.photos.last.image.url, size: "50x50" if product.photos.present?%>    
        </td>
        <td><%= product.name %></td>
        <td><%= product.category %></td>
        <td><%= product.price %></td>
        <td><%= product.cost %></td>
        <td><%= product.discount %></td>
        <td><%= product.quantity %></td>
        <td><%= product.description %></td>
        
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) if policy(Product).edit?%></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } if policy(Product).destroy? %></td>
        <td><%= link_to 'Add To Cart', add_to_cart_path(:product_id => product.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path if policy(Product).new? %>
